# sdlc-gate-app-test

Disposable test repository for exercising the **agentic-sdlc** pipeline
(spec → design → code/review loop → gates). Safe to delete at any time.

A tiny, dependency-free Python package so agents have a real codebase with
runnable tests.

## Test

```bash
python3 -m unittest discover
```
